# .github/workflows/ci.yml
name: 🧪 CI with MySQL

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  tests:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: secret
          MYSQL_DATABASE: homestead
          MYSQL_USER: homestead
          MYSQL_PASSWORD: secret
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3
        ports:
          - 3306:3306

    env:
      DB_CONNECTION: mysql
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      DB_DATABASE: homestead
      DB_USERNAME: homestead
      DB_PASSWORD: secret
      APP_ENV: testing
      QUEUE_CONNECTION: sync
      MAIL_MAILER: array

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up PHP 8.2
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.2
          extensions: mbstring, pdo_mysql, zip
          ini-values: post_max_size=256M, upload_max_filesize=256M

      - name: Install Composer dependencies
        run: composer install --prefer-dist --no-progress --no-suggest --ansi

      - name: Copy .env for tests
        run: cp .env.example .env

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
        node-version: 18
      - name: Install NPM dependencies
        run: npm ci
      - name: Build assets
        run: npm run build


      - name: Generate application key
        run: php artisan key:generate --no-interaction

      - name: Wait for MySQL service
        run: |
          for i in $(seq 1 10); do
            mysqladmin ping -h 127.0.0.1 -u homestead -psecret && break
            echo 'Waiting for MySQL…' && sleep 5
          done

      - name: Run migrations
        run: php artisan migrate --force

      - name: Run PHPUnit
        run: composer test

      # Décommente si tu as des tests JS ou un lint à passer
      #- name: Setup Node.js
      #  uses: actions/setup-node@v3
      #  with:
      #    node-version: 18
      #
      #- name: Install NPM dependencies
      #  run: npm ci
      #
      #- name: Run JS tests / lint
      #  run: npm run lint
